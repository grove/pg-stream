# PLAN_0_11_0.md — v0.11.0 Implementation Order

**Milestone:** v0.11.0 — Partitioned Stream Tables, Prometheus & Grafana Observability, Safety Hardening & Correctness
**Last updated:** 2026-03-25
**Phase 1 status:** ✅ Complete (merged PR #279, 2026-03-25)
**Phase 2 status:** ✅ Complete (PR #TBD, 2026-03-26)
**Phase 3 status:** ✅ Complete (PR #282, 2026-03-26)
**Phase 4 status:** ✅ Complete (PR #283, 2026-03-25)
**Phase 6 status:** ✅ Complete

This document defines the recommended implementation order for all v0.11.0
roadmap items. Sequencing is driven by:

1. **Correctness and safety first** — panics and silent failures must be
   eliminated before adding new code paths.
2. **Dependency order** — later phases build on infrastructure introduced by
   earlier ones (e.g. WB-1 must land before ST-to-ST; WAKE-1 before C3-1).
3. **Risk front-loading** — design spikes for high-risk items (partitioning)
   run in parallel with Phase 1 so go/no-go decisions are made early.
4. **Low effort, high value early** — cheap quick wins ship immediately and
   improve every subsequent development cycle.

---

## Phase 1 — Quick Wins & Safety Foundation (✅ Complete)

All items shipped in PR #279 (branch `0.11-phase-1`). 1454 unit tests pass.

| Item | Description | Status |
|------|-------------|--------|
| QF-1 | Fix unguarded debug `println!` in `refresh.rs:2269`; replace with `pgrx::log!()` behind `pg_trickle.log_merge_sql` GUC | ✅ Done |
| QF-2 | Raise AUTO→FULL downgrade log from `info!()` to `warning!()` | ✅ Done |
| QF-3 | Emit `WARNING` when `append_only` silently reverts to `false` | ✅ Already present |
| QF-4 | Add `// INVARIANT:` comments to four `unwrap()` sites in `dvm/parser.rs` | ✅ Done |
| SAF-1 | Replace all `panic!`/`unwrap()` in background worker hot paths (`scheduler.rs`, `refresh.rs`, `hooks.rs`) with `PgTrickleError` propagation; log at `WARNING` and continue | ✅ Audited — none found; `check_skip_needed` hardened |
| DEF-4 | Increase invalidation ring capacity from 32 → 128 slots (one-line constant) | ✅ Done |
| DEF-1 | Flip `parallel_refresh_mode` default to `'on'`; unit test updated | ✅ Done (E2E test pending Phase 2) |
| DEF-5 | Flip `block_source_ddl` default to `true`; error message includes escape-hatch instructions | ✅ Done (E2E test pending Phase 2) |

**Phase 1 exit criteria met:**
- ✅ No `panic!`/`unwrap()` in worker hot paths (audit complete)
- ✅ DEF-1, DEF-4, DEF-5 defaults flipped
- ✅ QF-1 debug `println!` gone
- ✅ Ring capacity 128
- ✅ `just fmt` clean; `cargo clippy --lib` zero warnings; 1454 unit tests pass

---

## Phase 2 — Catalog Groundwork & API Hardening (✅ Complete)

All items shipped in branch `0.11-phase-2`. 1454 unit tests pass.

| Item | Description | Status |
|------|-------------|--------|
| G12-ERM-1 | Add `effective_refresh_mode` column to `pgt_stream_tables`; populate on each refresh via thread-local mode tracking | ✅ Done |
| G12-ERM-2 | Add `explain_refresh_mode(name TEXT)` SQL function — reports configured mode, effective mode, downgrade reason | ✅ Done |
| G15-PV | Validate incompatible parameter combinations at `create_stream_table` / `alter_stream_table` time: `cdc_mode='wal'` + `IMMEDIATE` (pre-existing); `diamond_schedule_policy='slowest'` without `diamond_consistency='atomic'` | ✅ Done (WAL+IMMEDIATE pre-existing; slowest+none added) |
| G13-EH | Add PostgreSQL `DETAIL` + `HINT` fields to `UnsupportedOperator`, `CycleDetected`, `UpstreamSchemaChanged`, and `QueryParseError` via `raise_error_with_context()` helper | ✅ Done |
| DEF-3 | SemiJoin delta-key pre-filter (O-1): inject `WHERE left_key IN (SELECT key FROM delta)` before full left-table scan | ✅ Already implemented (verified pre-existing in `semi_join.rs`) |
| SAF-2 | Failure-injection E2E test: schema-change injection; assert remaining STs continue refreshing and worker does not crash | ✅ Done (tests/e2e_safety_tests.rs) |

**Phase 2 exit criteria met:**
- ✅ `effective_refresh_mode` column added; populated by scheduler on every completed refresh
- ✅ `explain_refresh_mode()` SQL function available with configured/effective/downgrade reason
- ✅ `diamond_schedule_policy='slowest'` + `diamond_consistency='none'` now rejected with HINT
- ✅ `DETAIL` + `HINT` fields on all 4 targeted error variants
- ✅ SAF-2 failure-injection E2E tests in `tests/e2e_safety_tests.rs`
- ✅ Upgrade SQL `sql/pg_trickle--0.10.0--0.11.0.sql` created
- ✅ `cargo fmt` clean; `cargo clippy --lib` zero warnings; 1454 unit tests pass

---

## Phase 3 — Prometheus & Grafana Observability (✅ Complete)

All files land in `monitoring/` directory. Zero Rust/SQL code changes.

| Item | Description | Status |
|------|-------------|--------|
| OBS-1 | `postgres_exporter` YAML config: `monitoring/prometheus/pg_trickle_queries.yml` — exposes per-table refresh stats, health summary, CDC buffer sizes, status counts, and recent error rate | ✅ Done |
| OBS-2 | Prometheus alerting rules: `monitoring/prometheus/alerts.yml` — staleness > 5 min, ≥3 consecutive failures, table SUSPENDED, CDC buffer > 1 GB, scheduler down, high refresh duration | ✅ Done |
| OBS-3 | Grafana dashboard JSON: `monitoring/grafana/dashboards/pg_trickle_overview.json` — overview stat panels, refresh performance time-series, staleness heatmap, CDC health graphs, per-table drill-down table | ✅ Done |
| OBS-4 | `monitoring/docker-compose.yml` demo stack: PostgreSQL + pg_trickle + postgres_exporter + Prometheus + Grafana, pre-wired; includes `monitoring/init/01_demo.sql` seed script | ✅ Done |

**Phase 3 exit criteria met:**
- ✅ `monitoring/prometheus/pg_trickle_queries.yml` exports 14 metrics covering all key ST and CDC dimensions
- ✅ `monitoring/prometheus/alerts.yml` has 8 alerting rules covering staleness, errors, CDC lag, and scheduler health
- ✅ `monitoring/grafana/dashboards/pg_trickle_overview.json` pre-provisioned dashboard with 6 sections
- ✅ `monitoring/docker-compose.yml` — `docker compose up` lights up Grafana at :3000 with live data
- ✅ `monitoring/README.md` documents all metrics and alert conditions

---

## Phase 4 — Correctness Guards & Regression Net (✅ Complete)

All items shipped in branch `0.11-phase-4`. 1474 unit tests pass.

| Item | Description | Status |
|------|-------------|--------|
| G12-2 | TopK runtime validation: `validate_topk_metadata()` re-parses the reconstructed full query on each refresh, verifies LIMIT/OFFSET metadata matches stored catalog; falls back to FULL + `WARNING` on mismatch. `validate_topk_metadata_fields()` is pure logic (unit-testable). | ✅ Done |
| G12-AGG | Warn at `create_stream_table` time when `DIFFERENTIAL` mode is used with group-rescan aggregates (`STRING_AGG`, `ARRAY_AGG`, etc.). `classify_agg_strategy()` classifies each aggregate as ALGEBRAIC_INVERTIBLE / ALGEBRAIC_VIA_AUX / SEMI_ALGEBRAIC / GROUP_RESCAN. Strategy exposed via `explain_st()` as `aggregate_strategies` JSON property. | ✅ Done |
| G17-EC01B-NEG | Negative regression tests in `join_common.rs` asserting ≥3-scan join right subtrees fall back to post-change snapshot (not phantom-row drop). 4 tests covering 3-way, 4-way, right-subtree, and 2-scan boundary. `// TODO: Remove when EC01B-1/EC01B-2 fixed in v0.12.0` | ✅ Done |

**Phase 4 exit criteria met:**
- ✅ TopK runtime guard active (`validate_topk_metadata` + `validate_topk_metadata_fields`)
- ✅ Group-rescan aggregate warning emitted at `create_stream_table` time
- ✅ `classify_agg_strategy()` with full unit test coverage (18 tests)
- ✅ `explain_st()` exposes `aggregate_strategies` JSON property
- ✅ EC-01 regression tests in CI (4 tests in `join_common.rs`)
- ✅ TopK metadata validation unit tests (7 tests in `refresh.rs`)
- ✅ `cargo fmt` clean; `cargo clippy --lib` zero warnings; 1474 unit tests pass

---

## Phase 5 — Wider Changed-Column Bitmask (~1–2 weeks)

**Must land before ST-to-ST** — ST-to-ST constructs CDC infrastructure on top
of the change buffer schema.

| Item | Description | Effort |
|------|-------------|--------|
| WB-1 | Extend `changed_cols` from `BIGINT` to `BYTEA` in CDC trigger and `cdc.rs`; schema migration for existing change buffer tables; tables with <64 columns are data-level unaffected | 1–2 wk |
| WB-2 | E2E test: >63-column source table; verify only referenced columns trigger delta propagation; benchmark UPDATE selectivity before/after | 2–4h |

**Exit:** Wide-table CDC selectivity E2E passes. Schema migration tested on
existing deployments.

---

## Phase 6 — Fuse Circuit Breaker (✅ Complete)

All items shipped in branch `0.11-phase-6`.

| Item | Description | Status |
|------|-------------|--------|
| FUSE-1 | Catalog: add fuse state columns to `pgt_stream_tables` (`fuse_mode`, `fuse_state`, `fuse_ceiling`, `fuse_sensitivity`, `blown_at`, `blow_reason`). Upgrade SQL migration with CHECK constraints. | ✅ Done |
| FUSE-2 | `alter_stream_table()` new params: `fuse`, `fuse_ceiling`, `fuse_sensitivity`. Validation rejects non-positive ceilings. | ✅ Done |
| FUSE-3 | `reset_fuse(name, action => 'apply'|'reinitialize'|'skip_changes')` SQL function. All three action modes implemented with appropriate catalog + change buffer operations. | ✅ Done |
| FUSE-4 | `fuse_status()` introspection function returning per-ST fuse configuration and state via `TableIterator`. | ✅ Done |
| FUSE-5 | Scheduler pre-check: `count_pending_changes()` + `evaluate_fuse()` injected in all 5 scheduler execution paths (singleton, atomic group, immediate closure, diamond inline, main inline). Blows fuse + `pg_notify('pgtrickle_alert', …)` when ceiling exceeded. Per-ST ceiling overrides global `pg_trickle.fuse_default_ceiling` GUC. 6 unit tests for `fuse_is_active()` pure logic. | ✅ Done |
| FUSE-6 | E2E tests: 9 test cases in `tests/e2e_fuse_tests.rs` — fuse-off baseline, spike→blow, reset apply/reinitialize/skip_changes, fuse_status introspection, alter validation, reset-not-blown errors, global ceiling GUC. | ✅ Done |

**Phase 6 exit criteria met:**
- ✅ Fuse blows on configurable per-ST or global ceiling threshold
- ✅ All three `reset_fuse()` action modes implemented and tested
- ✅ `fuse_status()` introspection function available
- ✅ `pg_notify('pgtrickle_alert', ...)` emitted on fuse blow
- ✅ Scheduler pre-check in all 5 execution paths
- ✅ `pg_trickle.fuse_default_ceiling` GUC for global default
- ✅ Upgrade SQL migration in `sql/pg_trickle--0.10.0--0.11.0.sql`
- ✅ `cargo fmt` clean; `cargo clippy --lib` zero warnings

---

## Phase 7 — Adaptive / Event-Driven Scheduler Wake (~2–3 weeks)

Must-ship (low latency is a primary project goal). Lands after Fuse (both
modify the scheduler entry point) and before ST-to-ST (which adds new CDC
wake signals).

| Item | Description | Effort |
|------|-------------|--------|
| WAKE-1 | `pg_notify('pgtrickle_wake', '')` from CDC triggers; 10 ms coalesce debounce; poll-based fallback when idle. Expected: median latency 515 ms → 15 ms (34×) | 2–3 wk |

**Exit:** Median end-to-end latency ≤ 20 ms for low-volume workloads.
Event-driven and poll-based paths both covered by E2E tests.

---

## Phase 8 — Differential ST-to-ST Refresh (~4.5–6.5 weeks)

The largest item. Builds on CDC infrastructure (WB-1), scheduler stability
(WAKE-1, FUSE), and catalog work (G12-ERM). Implement in strict sub-phase
order — each sub-phase is a reviewable unit.

| Sub-phase | Item | Description | Effort |
|-----------|------|-------------|--------|
| 8.1 | ST-ST-1 | Change buffer infrastructure: `create_st_change_buffer_table()` / `drop_st_change_buffer_table()` in `cdc.rs`; lifecycle hooks in `api.rs`; auto-create on upgrade | 1 wk |
| 8.2 | ST-ST-2 | Delta capture — DIFFERENTIAL path: `MERGE … RETURNING` to atomically apply MERGE and write delta to `changes_pgt_{id}` | 1–2 wk |
| 8.3 | ST-ST-3 | Delta capture — FULL path: pre/post snapshot diff; downstream STs always receive I/D delta regardless of upstream mode | 1 wk |
| 8.4 | ST-ST-4 | DVM scan operator for ST sources: read from `changes_pgt_{id}` instead of `changes_{oid}`; `pgt_`-prefixed LSN tokens; extend frontier computation | 1–2 wk |
| 8.5 | ST-ST-5 | Scheduler integration: rewrite `has_stream_table_source_changes()` to check ST buffer; remove `has_stream_table_changes → Full` override when buffer exists | 0.5 wk |
| 8.6 | ST-ST-6 | Cleanup, lifecycle, tests: extend `drain_pending_cleanups()`; hook `hooks.rs` for DDL; unit + integration + E2E tests (3-level chain, DROP-middle fallback, schema change buffer recreation, min-frontier cleanup) | 1 wk |

**Exit:** ST → ST → ST 3-level chain refreshes differentially. Pre/post
snapshot path means no cascading FULL. DROP-middle-ST falls back gracefully.

---

## Phase 9 — External Correctness Gate (~1–3 days)

Run after ST-to-ST to catch any regressions it introduced. Gates CI before
the 0.11.0 exit criteria are signed off.

| Item | Description | Effort |
|------|-------------|--------|
| TS1 **(preferred)** | Run PostgreSQL sqllogictest suite through pg_trickle DIFFERENTIAL mode; gate CI on zero correctness mismatches | 2–3d |
| TS2 *(alternative)* | JOB (Join Order Benchmark): correctness baseline + refresh latency profiling on realistic multi-join analytical queries | 1–2d |

Deliver **one** of TS1 or TS2; whichever completes first meets the exit
criterion.

**Exit:** ≥1 external corpus passes with zero correctness mismatches in
DIFFERENTIAL mode; gated in CI.

---

## Parallel Track — Partitioning Design Spike (start during Phase 1)

**Start STRETCH-1 early** — run as a background design activity during
Phase 1/2 so the go/no-go decision is available before committing significant
effort.

| Item | Description | Effort |
|------|-------------|--------|
| STRETCH-1 | Produce a written implementation plan: validates partition-key predicate injection, per-partition MERGE loop feasibility, catalog requirements, and migration approach. RFC must be approved before A1-1 starts. | 2–4d |

Only proceed to A1-1 through A1-4 (full partitioning implementation) if the
spike confirms feasibility and the RFC is approved. A1-1 through A1-4 are then
scheduled between Phase 8 and Phase 9.

**C3-1** (per-database worker quotas) depends on the scheduler rework in
WAKE-1 — schedule it after Phase 7.

---

## Documentation (thread throughout)

Write documentation incrementally alongside the code phases rather than in a
single block at the end.

| Item | Best time to write |
|------|--------------------|
| G16-SM (SQL/mode operator support matrix in `DVM_OPERATORS.md`) | After Phase 3 (OBS), before Phase 8 (ST-to-ST) |
| G16-MQR (Monitoring quick reference in `GETTING_STARTED.md`) | Alongside Phase 3 (OBS) |
| G15-GUC (GUC interaction matrix + tuning profiles in `CONFIGURATION.md`) | After Phase 2 (catalog) |
| G16-GS (Restructure `GETTING_STARTED.md` progressive complexity) | After Phase 9 (final feature set known) |

---

## Summary Timeline

| Phase | Content | Approx. Duration |
|-------|---------|-----------------|
| 1 | Quick wins & safety foundation | ~1 week |
| 2 | Catalog groundwork & API hardening | ~1 week |
| 3 | Prometheus/Grafana observability | ~12 hours (overlaps Phase 2) |
| 4 | Correctness guards & regression net | ~1 week |
| 5 | Wider changed-column bitmask | ~1–2 weeks |
| 6 | Fuse circuit breaker | ~10–14 hours |
| 7 | Adaptive/event-driven scheduler wake | ~2–3 weeks |
| 8 | Differential ST-to-ST refresh | ~4.5–6.5 weeks |
| 9 | External correctness gate | ~1–3 days |
| ∥ | Partitioning design spike (parallel) | ~2–4 days |
| ∥ | Documentation (parallel throughout) | ~2–3 days |

**Total:** approximately 12–16 weeks of focused development. Phases 1–4 are
well-bounded and deliverable within the first month; Phases 5–9 carry the
bulk of the complexity.

---

## v0.11.0 Exit Criteria (from ROADMAP.md)

- [ ] Declaratively partitioned stream tables accepted; partition key tracked in catalog *(if STRETCH-1 spike approved)*
- [ ] Partition-scoped MERGE benchmark: 10M-row ST, 0.1% change rate (expect ~100× I/O reduction) *(if implemented)*
- [ ] Per-database worker quotas enforced; burst reclaimed within 1 scheduler cycle (C3-1)
- [ ] Prometheus queries + alerting rules + Grafana dashboard shipped (OBS-1–4)
- [ ] DEF-1: `parallel_refresh_mode` default is `'on'`; concurrent-refresh E2E test passes
- [x] DEF-2: `auto_backoff` default is `true` — ✅ Done in v0.10.0
- [ ] DEF-3: SemiJoin delta-key pre-filter implemented; TPC-H Q18/Q20/Q21 re-benchmarked
- [ ] DEF-4: Invalidation ring capacity is 128 slots; validated under rapid DDL E2E test
- [ ] DEF-5: `block_source_ddl` default is `true`; error message includes escape-hatch instructions; E2E test verifies block and escape hatch
- [ ] SAF-1+2: No `panic!`/`unwrap()` in background worker hot paths; failure-injection E2E test passes
- [ ] WB-1+2: Changed-column bitmask supports >63 columns; wide-table CDC selectivity E2E passes; schema migration tested
- [ ] FUSE-1–6: Fuse blows on configurable change-count threshold; `reset_fuse()` recovers in all three action modes; diamond/DAG interaction tested
- [ ] TS1 or TS2: At least one external query corpus passes with zero correctness mismatches in DIFFERENTIAL mode
