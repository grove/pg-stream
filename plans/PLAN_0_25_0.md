# PLAN_0_25_0.md — v0.25.0 Implementation Order

**Milestone:** v0.25.0 — Scheduler Scalability & Pooler Performance
**Status:** 🚧 In progress
**Last updated:** 2026-04-21

This document defines the recommended implementation order for all v0.25.0
roadmap items. See [ROADMAP.md §v0.25.0](../ROADMAP.md#v0250--scheduler-scalability--pooler-performance)
for the full feature descriptions and effort estimates.

---

## Milestone Goals

- Push the comfortable operating point from "hundreds" to **thousands** of stream tables
- Eliminate per-tick SPI catalog reload via shmem snapshot cache (SCAL-1)
- Batch change detection across a refresh group into one query (SCAL-2)
- Split the monolithic `PGS_STATE` lock into per-concern locks (SCAL-3)
- Eliminate lock contention during DAG rebuild via copy-on-write (SCAL-4)
- Reduce spawn overhead with a persistent worker pool option (SCAL-5)
- Eliminate 30–45 ms cold-start tax in pooled-connection deployments (CACHE-1)
- Bound the per-backend template cache with LRU eviction (CACHE-2)
- Provide manual cache flush for debugging and migration (CACHE-3)
- Eliminate per-row String allocation in `pg_trickle_hash_multi` (PERF-1)
- Reduce allocations in the project operator hot path (PERF-2)
- Read cost-model state from shmem instead of SPI in parallel workers (PERF-3)
- Add robustness guards to the predictive cost model (PRED-1)
- Refuse buffer truncation until subscribers acknowledge (PUB-1)
- Add per-DB worker allocation monitoring view (PUB-2)

---

## Recommended Implementation Order

### Phase 1 — Quick wins (0.5–1d each)

1. **PERF-2**: Pre-sized SQL buffer in project operator  
2. **CACHE-3**: `pgtrickle.clear_caches()` SQL function  
3. **PUB-2**: `worker_allocation_status()` monitoring view  

### Phase 2 — Hot-path allocation & hash (3d)

4. **PERF-1**: xxh3 streaming hash  
5. **PERF-3**: Shmem adaptive cost-model state  

### Phase 3 — Predictive model & L1 cache (5d)

6. **PRED-1**: Robustness guards on predictive cost model  
7. **CACHE-2**: L1 LRU eviction  

### Phase 4 — Catalog & scheduler scalability (11d)

8. **SCAL-2**: Batched change detection  
9. **SCAL-3**: Split PGS_STATE lock  
10. **SCAL-1**: Shmem catalog snapshot cache  
11. **SCAL-4**: Copy-on-write DAG rebuild  
12. **SCAL-5**: Persistent worker pool option  

### Phase 5 — Template cache & publication durability (11d)

13. **CACHE-1**: Shared shmem L0 template cache  
14. **PUB-1**: Subscriber-LSN tracking  

### Phase 6 — Benchmarks, docs, upgrade script, integration testing (7d)

- Benchmarks: diff_operators bench, refresh bench
- Documentation: `docs/CONFIGURATION.md`, `docs/SCALING.md`
- SQL upgrade script: `sql/pg_trickle--0.24.0--0.25.0.sql`
- Integration testing: `just test-integration`

---

## Implementation Status

| ID | Feature | Status |
|----|---------|--------|
| PERF-2 | Pre-sized SQL buffer in project operator | ✅ Done |
| CACHE-3 | `pgtrickle.clear_caches()` SQL function | ✅ Done |
| PUB-2 | `worker_allocation_status()` monitoring view | ✅ Done |
| PERF-1 | xxh3 streaming hash | ✅ Done |
| PERF-3 | Shmem adaptive cost-model state | ✅ Done |
| PRED-1 | Robustness guards on predictive cost model | ✅ Done |
| CACHE-2 | L1 LRU eviction | ✅ Done |
| SCAL-2 | Batched change detection | ✅ Done |
| SCAL-3 | Split PGS_STATE lock | ✅ Done |
| SCAL-1 | Shmem catalog snapshot cache | ✅ Done |
| SCAL-4 | Copy-on-write DAG rebuild | ✅ Done |
| SCAL-5 | Persistent worker pool option | ✅ Done |
| CACHE-1 | Shared shmem L0 template cache | ✅ Done |
| PUB-1 | Subscriber-LSN tracking | ✅ Done |

---

## Release Exit Criteria

- [x] SCAL-1: Scheduler tick at 1000 STs completes in < 20 ms (down from ~200 ms)
- [x] SCAL-2: Change detection for 10-source ST issues 1 query instead of 10
- [x] SCAL-3: PGS_STATE replaced by 3 per-concern locks; read-only paths use `share()`
- [x] SCAL-4: DAG rebuild does not hold exclusive lock during computation; swap is atomic
- [x] SCAL-5: `worker_pool_size = 4` starts persistent workers; spawn cost eliminated
- [x] CACHE-1: Second backend connecting to same DB hits L0 cache; no parse/differentiate cost
- [x] CACHE-2: L1 cache respects `template_cache_max_entries`; evicts LRU on overflow
- [x] CACHE-3: `pgtrickle.clear_caches()` flushes all three levels; next refresh re-populates
- [x] PERF-1: `pg_trickle_hash_multi` allocates zero intermediate Strings per row
- [x] PERF-2: Project operator uses single pre-sized buffer; 50-column ST shows measurable improvement
- [x] PERF-3: Parallel workers read cost-model state from shmem, not SPI
- [x] PRED-1: Sawtooth workload test: model recovers within 5 samples after outlier spike
- [x] PUB-1: Publication with lagged subscriber emits WARNING; change buffer not truncated until ack
- [x] PUB-2: `worker_allocation_status()` returns per-DB used/quota/queued
- [x] Benchmark regression gate passes (no regressions vs v0.24.0 baseline)
- [x] Extension upgrade path tested (`0.24.0 → 0.25.0`)
- [x] `just check-version-sync` passes
