> **Plain-language companion:** [v0.43.0.md](v0.43.0.md)

## v0.43.0 — Test Quality, Aggregate Correctness & Fuzz Automation

**Status: Planned.** Derived from [plans/PLAN_OVERALL_ASSESSMENT_9.md](../plans/PLAN_OVERALL_ASSESSMENT_9.md) §Dimension 1 (Correctness), §Dimension 6 (Test Coverage), §Dimension 10 (CI).

> **Release Theme**
> Dramatically improve test trust by replacing fixed sleeps, adding property-
> style aggregate correctness tests, hardening WAL decoder SQL, and automating
> fuzz targets in CI.

---

### Features

| ID | Title | Effort | Priority | Assessment ref |
|----|-------|--------|----------|----------------|
| A43-1 | Replace fixed sleeps with state-polling helpers | L | P0 | TEST-01 |
| A43-2 | Differential SUM(CASE) E2E tests | M | P0 | TEST-02, COR-04 |
| A43-3 | SUM(CASE) AST-level detection in aggregate classification | M | P1 | COR-04 |
| A43-4 | FULL JOIN aggregate property tests (DIFF vs FULL) | L | P1 | COR-05 |
| A43-5 | WAL decoder SQL parameterization | M | P1 | COR-07 |
| A43-6 | Update stale EC-06 comments | S | P2 | COR-08 |
| A43-7 | Keyless multiset property test | S | P2 | TEST-09 |
| A43-8 | Fuzz smoke CI job with corpus replay | M | P1 | TEST-06, CI-05 |

**A43-1 — Replace fixed sleeps with state-polling helpers.**
Create shared test helpers in `tests/common/mod.rs`:
- `wait_for_refresh_history(client, st_name, min_count, timeout)` — polls
  refresh history until the expected row count is reached.
- `wait_for_cdc_mode(client, st_name, expected_mode, timeout)` — polls CDC
  mode in catalog.
- `wait_for_scheduler_tick(client, min_tick, timeout)` — polls scheduler
  watermark.
- `wait_for_job_status(client, job_id, expected_status, timeout)` — polls job.

Replace the highest-impact sleeps (WAL CDC, scheduler, cascade, PgBouncer,
quota, bgworker tests) with these helpers. Target: reduce the 116 fixed sleeps
by at least 80%.

**A43-2 — Differential SUM(CASE) E2E tests.**
Add E2E tests in DIFF mode for `SUM(CASE WHEN x > threshold THEN y ELSE 0 END)`
with:
- INSERT rows above and below threshold
- UPDATE rows crossing the threshold in both directions
- DELETE qualifying and non-qualifying rows
- Multi-cycle sequences

After each operation, compare the stream table contents with a fresh full query
result.

**A43-3 — SUM(CASE) AST-level detection.**
In `src/dvm/operators/aggregate.rs`, detect CASE expressions at the parsed AST
level rather than matching the trimmed string prefix. Handle CASE wrapped in
casts, functions, or type coercions. Normalize the expression tree before
classification.

**A43-4 — FULL JOIN aggregate property tests.**
Add property-style E2E tests that compare DIFF vs FULL refresh results for:
- Nested FULL JOIN + aggregate combinations
- Multi-cycle insert/update/delete sequences
- NULL keys and both-side changes in the same cycle
- Multiple aggregate functions (SUM, COUNT, AVG) over FULL JOIN results

Use randomized data generation with fixed seeds for reproducibility.

**A43-5 — WAL decoder SQL parameterization.**
In `src/wal_decoder.rs`:
- Parameterize every value in `write_decoded_change` INSERT SQL using SPI
  parameters instead of manual escaping.
- Centralize slot-name construction with a strict grammar assertion.
- Add tests with quotes, backslashes, unicode, large text, nulls, and
  bytea-like payloads.

**A43-6 — Update stale EC-06 comments.**
Replace all comments in `src/dvm/operators/scan.rs` that describe EC-06 as
unfixed with current design notes explaining keyless net-counting, non-unique
row-id indexes, and the existing test coverage in
`tests/e2e_keyless_duplicate_tests.rs`.

**A43-7 — Keyless multiset property test.**
Add a small property test that generates random multisets of rows, applies
random insert/delete/update operations, and verifies that the keyless stream
table maintains multiset equivalence with the source query after each cycle.

**A43-8 — Fuzz smoke CI job.**
Add `.github/workflows/fuzz-smoke.yml` that:
- Runs on schedule (daily) and manual dispatch
- Executes each fuzz target with a short time budget (30-60 seconds)
- Replays the corpus on PRs (fast, deterministic)
- Uploads crashes and minimized repros as artifacts

### Test Coverage

| ID | Title | Effort | Priority | Assessment ref |
|----|-------|--------|----------|----------------|
| T-A43-1 | Polling helper coverage for all wait-state types | M | P0 | TEST-01 |
| T-A43-2 | SUM(CASE) differential multi-cycle test | M | P0 | TEST-02 |
| T-A43-3 | FULL JOIN nested aggregate DIFF-vs-FULL property test | L | P1 | COR-05 |
| T-A43-4 | WAL decoder payload edge-case tests | M | P1 | COR-07 |
| T-A43-5 | Fuzz corpus replay in CI | S | P1 | TEST-06 |

### Conflicts & Risks

- **A43-1** (sleep replacement) touches many test files. Risk of introducing
  new flakiness if polling timeouts are too aggressive. Use generous defaults
  (30s) with configurable overrides.
- **A43-3** (AST detection) changes aggregate classification, which affects
  DVM plan selection. Add golden-file tests for classification decisions.
- **A43-5** (WAL parameterization) changes SQL generation in a correctness-
  critical path. Run full WAL CDC E2E suite before and after.

### Exit Criteria

- [ ] A43-1: Fixed sleeps reduced by >= 80% (from 116 baseline)
- [ ] A43-2: SUM(CASE) differential tests pass with multi-cycle UPDATE/DELETE
- [ ] A43-3: CASE detection works for wrapped/cast/nested CASE expressions
- [ ] A43-4: FULL JOIN aggregate property tests converge DIFF == FULL
- [ ] A43-5: WAL decoder tests pass with adversarial payloads
- [ ] A43-8: Fuzz smoke CI runs without crashes on current corpus
- [ ] Extension upgrade path tested (`0.42.0 → 0.43.0`)
- [ ] `just lint` passes with zero warnings
- [ ] `just test-all` passes (including new property tests)
